apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metamorph.fullname" . }}-test-connection"
  labels:
{{ include "metamorph.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "metamorph.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
